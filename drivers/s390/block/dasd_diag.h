/* SPDX-License-Identifier: GPL-2.0 */
/*
 * Author(s)......: Holger Smolinski <Holger.Smolinski@de.ibm.com>
 * Based on.......: linux/drivers/s390/block/mdisk.h
 * ...............: by Hartmunt Penner <hpenner@de.ibm.com>
 * Bugreports.to..: <Linux390@de.ibm.com>
 * Copyright IBM Corp. 1999, 2000
 *
 */

/* [한국어] [한국어 설명] z/VM DIAG 250 디시플린의 전용 헤더 (dasd_diag.h)
 * 
 * === 파일의 역할 ===
 * DASD 드라이버는 장치 종류마다 디시플린(discipline)이라는 가상 함수표를 갈아 끼우는
 * 구조인데, 그중 DIAG 판이 쓰는 어휘를 모아 둔 곳이 이 헤더다. 담긴 것은 넷이다.
 * 첫째, DIAG 명령의 **기능 코드** 셋(INIT_BIO / RW_BIO / TERM_BIO)과 블록 하나의
 * **요청 종류** 둘(MDSK_READ_REQ / MDSK_WRITE_REQ). 둘째, 하이퍼바이저에게 건네는
 * **파라미터 블록 구조체** 셋 — 장치 특성(12바이트), 환경 초기화·해제 인자(64바이트),
 * 읽기·쓰기 인자(64바이트). 셋째, 그 읽기·쓰기 인자가 가리키는 **블록 서술자**
 * struct dasd_diag_bio(24바이트) 하나. 넷째, 완료 인터럽트의 부코드 값 둘과 플래그 비트들.
 * 함수 정의도 #include 도 하나 없고, 오직 dasd_diag.c 만 이 헤더를 포함한다
 * (drivers 전체 grep 결과 dasd_diag.c 한 곳뿐이다). 아래에서 dasd_diag.c 의 코드를
 * 가리킬 때 줄 번호 대신 함수 이름을 쓴 까닭은, 그 파일도 같은 작업으로 주석이
 * 붙어 줄 번호가 옮겨 가기 때문이다. 줄 번호로 짚은 인용은 모두 다른 파일의 것이다.
 * 
 * 이 헤더를 읽을 때 가장 중요한 태도는 **여기 있는 구조체가 코드가 아니라
 * 하이퍼바이저와 주고받는 약속이라는 점** 이다. ECKD 헤더의 구조체가 채널과 제어
 * 장치가 읽는 바이트열이었다면, 이쪽 구조체는 **z/VM 이 읽는 바이트열** 이다.
 * packed 와 aligned 속성, 필드 순서, spare 배열의 길이가 전부 의미를 지니며, 필드
 * 하나를 옮기면 그 순간 z/VM 이 엉뚱한 자리를 읽는다. 다만 각 필드가 하이퍼바이저
 * 쪽에서 정확히 어떤 뜻인지는 z/VM 의 DIAG 250 인터페이스 문서 소관이고, 이 트리는
 * sparse checkout 이라 arch/s390 조차 없으므로 **이 트리에서 확인 못 함** 으로 적었다.
 * 확인 가능한 것은 dasd_diag.c 가 각 필드를 어떻게 채우고 어떻게 읽는지뿐이며,
 * 아래 필드 주석의 설정자·읽는 자는 전부 그 사용처를 줄 번호로 짚은 것이다.
 * 
 * === 전체 아키텍처에서의 위치 ===
 * DIAG 디시플린은 리눅스가 z/VM 게스트로 돌 때, 볼륨이 z/VM 미니디스크이고
 * sysfs 속성 use_diag 가 1 일 때 쓰인다(dasd_devmap.c:1102 의 상류 주석이
 * 'ssch 대신 diag 를 쓸지 고르는 속성' 이라고 밝힌다). 이때 드라이버는
 * **CCW 채널 프로그램을 만들지 않고, 채널 서브시스템을 부르지도 않는다.**
 * 대신 이 헤더의 구조체를 채우고 DIAG 명령 한 번을 실행해, 하이퍼바이저에게
 * '이 블록들을 읽어/써 달라' 고 부탁한다.
 * 
 * 블록 요청 하나가 하이퍼바이저에 닿기까지의 길은 이렇다.
 * 
 *   파일시스템 / 사용자 공간
 *     -> blk-mq -> dasd.c:3018 의 요청 처리 함수
 *       -> discipline->build_cp == dasd_diag_build_cp
 *          [struct dasd_diag_bio 배열을 블록 개수만큼 채운다]
 *         -> struct dasd_ccw_req 의 data 에 매달린다 (cpaddr 은 끝내 NULL 이다)
 *           -> dasd.c 의 두 큐와 두 tasklet 을 거쳐
 *             -> discipline->start_IO == dasd_start_diag
 *                [struct dasd_diag_rw_io 를 채운다]
 *               -> DIAG 명령 실행 -> z/VM 이 실제 I/O 를 수행
 * 
 * 완료는 **채널 인터럽트가 아니라 외부 인터럽트(external interrupt)** 로 돌아온다.
 * dasd_diag.c 의 dasd_ext_handler 가 그것을 받아 부코드를 보고 요청을 되찾는다.
 * 
 *   z/VM -> 외부 인터럽트 (서비스 신호)
 *     -> dasd_ext_handler                    [인터럽트 문맥]
 *       -> 부코드 상위 바이트로 인터럽트 인자의 폭(31비트/64비트)을 가린다
 *         -> 그 인자가 곧 struct dasd_ccw_req 의 주소다
 *           -> cqr->status 를 갱신하고 device tasklet 을 깨운다
 * 
 * === 타 모듈과의 연결 ===
 * 위로는 dasd_int.h 에 기댄다. struct dasd_device 의 private 포인터가
 * dasd_diag.c 의 struct dasd_diag_private 을 가리키고, 그 안에 이 헤더의 구조체
 * 셋 중 둘(iob, iib)이 **장치마다 하나씩 미리 박혀 있다.** 요청마다 새로 잡지 않고
 * 장치에 붙박이로 두는 까닭은, DIAG 호출이 장치당 한 번에 하나씩만 나가기 때문이다.
 * DASD_DIAG_MAGIC(dasd_int.h:266, 값 0xC4C9C1C7)과 DASD_CQR_ 계열 상태 상수,
 * 디시플린 vtable 정의도 모두 그쪽 소관이다.
 * 
 * 아래로는 s390 아키텍처 계층에 기대는데, **이 트리에는 arch/s390 이 없다.**
 * DIAG 명령 자체의 의미, struct diag210(장치 특성을 읽어 오는 진단 호출의 인자),
 * struct ext_code(외부 인터럽트 부코드), addr_t, 그리고 z/VM 이 이 파라미터 블록을
 * 어떻게 해석하는지가 전부 그 계층 소관이라 이 트리에서 확인 못 함이다.
 * 
 * 옆으로는 dasd.c 와 전역 포인터 하나로만 이어진다. DIAG 디시플린은 별도 모듈
 * (Makefile 의 dasd_diag_mod)이라, 코어 모듈이 이름으로 직접 참조할 수 없다.
 * 그래서 코어가 dasd.c:46 에 변수를 두고, DIAG 모듈이 올라올 때 자기 vtable 주소를
 * 거기 써 넣는 방식으로 서로를 찾는다. 자세한 사정은 dasd_diag.c 의 상단 블록에 적었다.
 * 
 * 데이터 흐름은 두 방향이다. 나가는 쪽은 드라이버가 채운 값 — 장치 번호,
 * 블록 크기, 오프셋, 블록 서술자 배열의 주소, 완료 때 돌려받을 인터럽트 인자 — 이
 * z/VM 으로 간다. 들어오는 쪽은 z/VM 이 채운 값 — 장치의 마지막 블록 번호,
 * 블록마다의 상태, 장치 특성, 그리고 완료 인터럽트의 부코드 — 가 돌아온다.
 * 
 * === 주요 함수/구조체 요약 ===
 * struct dasd_diag_characteristics
 *                       장치 특성 12바이트. 장치 번호와 가상·실제 장치의
 *                       종류·모델이 들어 있다. 드라이버는 이것을 채워 진단 호출에
 *                       넘기고, 돌아온 vdev_class 로 레이블 블록 위치를 정한다.
 * struct dasd_diag_bio  블록 하나를 가리키는 서술자 24바이트. 종류(읽기/쓰기),
 *                       상태, 블록 번호, 버퍼 주소. 이 배열이 DIAG 판의
 *                       '채널 프로그램' 에 해당한다.
 * struct dasd_diag_init_io
 *                       환경 초기화·해제 파라미터 64바이트. 블록 크기와 오프셋을
 *                       건네고, 돌아올 때 end_block 에 마지막 블록 번호를 받는다.
 * struct dasd_diag_rw_io
 *                       읽기·쓰기 파라미터 64바이트. 블록 서술자 배열의 주소와
 *                       개수, 그리고 완료 때 되돌려받을 인터럽트 인자를 건넨다.
 * blocknum_t            블록 번호의 자료형(u64). 64비트 형식을 못 박은 것이다.
 * INIT_BIO 계열 세 값   DIAG 명령의 둘째 피연산자로 들어가는 기능 코드.
 * MDSK_READ_REQ 계열    블록 서술자의 type 필드에 들어가는 요청 종류.
 * DEV_CLASS_FBA 계열    장치 특성이 돌려주는 가상 장치 종류.
 * 
 * === DIAG 경로가 CCW 채널 프로그램과 무엇이 다른가 ===
 * 같은 디렉터리의 ECKD·FBA 디시플린과 이 헤더를 나란히 놓으면 차이가 뚜렷하다.
 * 
 *  (1) **무엇을 만드는가.** ECKD·FBA 는 struct ccw1 을 이어 붙인 채널 프로그램을
 *      만들어 cqr->cpaddr 에 매단다. DIAG 는 채널 프로그램을 아예 만들지 않는다.
 *      dasd_diag_build_cp 가 dasd_smalloc_request 를 부를 때 CCW 개수 자리에 0 을
 *      넘기므로, cqr->cpaddr 은 끝까지 NULL 로 남는다
 *      (dasd.c:1162 의 조건이 그 인자가 0 이면 cpaddr 을 세우지 않고 넘어간다).
 *      대신 struct dasd_diag_bio 배열이 그 자리를 대신한다.
 * 
 *  (2) **누가 I/O 를 하는가.** CCW 경로에서는 채널 서브시스템이라는 하드웨어가
 *      명령을 실행하고 제어 장치가 디스크를 돌린다. DIAG 경로에서는 z/VM 이라는
 *      소프트웨어가 게스트를 대신해 I/O 를 낸다. 그래서 여기에는 익스텐트 정의도,
 *      레코드 위치 지정도, 트랙 개념도 없다. 남는 것은 '몇 번 블록을 어디로' 뿐이다.
 * 
 *  (3) **어떻게 시작하는가.** CCW 경로는 채널 시작 명령을 쓰지만, DIAG 경로는
 *      DIAG 명령 한 번으로 끝난다. 게다가 그 한 번이 **동기 완료로 끝날 수도 있다** —
 *      조건 코드 0 이면 I/O 가 이미 끝난 것이고, 8 이면 비동기로 시작만 된 것이다.
 *      dasd_start_diag 가 두 경우를 갈라 처리한다.
 * 
 *  (4) **어떻게 완료를 받는가.** CCW 경로는 I/O 인터럽트와 struct irb(상태와 센스
 *      데이터)를 받는다. DIAG 경로는 **외부 인터럽트** 를 받고, 딸려 오는 것은
 *      부코드 한 워드뿐이다. 센스 데이터가 없으므로 DIAG 디시플린의 센스 덤프
 *      콜백 dasd_diag_dump_sense 는 '해 줄 말이 없다' 는 메시지만 남긴다.
 * 
 *  (5) **오류 복구가 무엇인가.** ECKD 는 3990 제어 장치의 센스 바이트를 해석하는
 *      거대한 복구 상태 기계를 갖는다. DIAG 는 해석할 센스가 없으므로, 복구란 곧
 *      'DIAG 환경을 헐고 다시 세우는 것' 이다 — dasd_diag_erp 가 그 일을 한다.
 * 
 *  (6) **왜 인터럽트가 특별한가.** 외부 인터럽트는 장치별로 오는 것이 아니라
 *      시스템 전체에 하나의 처리기로 온다. 그래서 이 처리기는 인터럽트 인자로 받은
 *      주소를 struct dasd_ccw_req 로 믿고 읽되, 그 안의 매직 값이 디시플린 이름과
 *      맞는지 먼저 대조해 남의 인터럽트를 걸러 낸다. */
/* [한국어] 블록 서술자 struct dasd_diag_bio 의 type 필드에 넣는 '쓰기' 요청 값.
 * 이름의 MDSK 는 z/VM 의 미니디스크(minidisk)를 줄인 말이고, 이 파일이 그
 * CMS 미니디스크 드라이버에서 갈라져 나왔음을 상단 저작권 블록이 밝히고 있다.
 * dasd_diag.c 의 dasd_diag_build_cp 가 블록 계층 요청의 방향이 쓰기일 때
 * 이 값을 골라 배열의 모든 서술자에 같은 값으로 넣는다.
 * 값 0x01 이 하이퍼바이저 쪽에서 정확히 어떻게 해석되는지는 z/VM 인터페이스
 * 소관이라 이 트리에서 확인 못 함. 확인 가능한 것은 드라이버가 읽기와 쓰기를
 * 이 한 바이트로만 구분한다는 사실뿐이다. */
#define MDSK_WRITE_REQ 0x01
/* [한국어] 같은 type 필드에 넣는 '읽기' 요청 값.
 * 쓰기와 마찬가지로 dasd_diag_build_cp 가 넣지만, 이 값에는 쓰임이 하나 더 있다.
 * 장치를 처음 인식할 때 dasd_diag_check_device 가 레이블 블록 한 개를 읽어
 * 블록 크기를 알아내는데, 그 한 건짜리 서술자의 type 이 바로 이 값이다.
 * 읽기(0x02)가 쓰기(0x01)보다 큰 값인 점에 특별한 뜻은 없다 — 두 값은 비트
 * 플래그가 아니라 그냥 열거 값이며, 드라이버 어디에서도 OR 로 합치지 않는다. */
#define MDSK_READ_REQ  0x02

/* [한국어] DIAG 명령의 둘째 피연산자로 들어가는 **기능 코드** 첫째 — 블록 I/O 환경 열기.
 * dasd_diag.c 의 mdsk_init_io 가 struct dasd_diag_init_io 를 채운 뒤 이 코드로
 * 호출한다. 하이퍼바이저에게 '이 장치를 이 블록 크기로, 이 오프셋부터 다루겠다'
 * 고 등록하는 절차이며, 성공하면 end_block 에 마지막 블록 번호를 돌려받는다.
 * 이 등록을 마쳐야만 아래 RW_BIO 가 통한다 — 즉 DIAG 경로에는 CCW 경로에 없는
 * **세션 개념** 이 있다. 값 0x00 의 하이퍼바이저 쪽 정의는 이 트리에서 확인 못 함. */
#define INIT_BIO	0x00
/* [한국어] 기능 코드 둘째 — 실제 읽기·쓰기.
 * dasd_diag.c 에서 두 곳이 이 코드를 쓴다. 하나는 dasd_start_diag 로, 블록 계층
 * 요청 하나를 통째로 내보내는 정상 경로다. 다른 하나는 dasd_diag_check_device 로,
 * 블록 크기를 알아내려 레이블 블록 한 개만 동기로 읽는 인식 경로다.
 * 두 경로의 차이는 struct dasd_diag_rw_io 의 flags 에 비동기 비트를 세우는지에
 * 있다 — 정상 경로는 세우고, 인식 경로는 0 으로 두어 호출이 끝날 때까지 기다린다. */
#define RW_BIO		0x01
/* [한국어] 기능 코드 셋째 — 블록 I/O 환경 닫기.
 * dasd_diag.c 의 mdsk_term_io 가 쓴다. 쓰이는 자리가 셋인데 성격이 서로 다르다.
 * (1) 장치 인식 때, 앞서 남아 있을지 모르는 세션을 먼저 끊으려고.
 * (2) 요청 중단(term_IO 콜백)에서, 진행 중인 I/O 를 하이퍼바이저 쪽에서 지우려고.
 * (3) 오류 복구(dasd_diag_erp)에서, 세션을 헐고 다시 세우려고.
 * 즉 DIAG 판에는 '개별 요청만 취소' 라는 수단이 없어, 언제나 세션 전체를 끊는다. */
#define TERM_BIO	0x02

/* [한국어] 장치 특성이 돌려주는 vdev_class 값 중 하나 — 고정 블록(FBA) 형식의 가상 장치.
 * 설정자는 하이퍼바이저다(진단 호출이 struct dasd_diag_characteristics 를 채워 준다).
 * 읽는 자는 dasd_diag_check_device 로, 이 값을 보고 CMS 레이블 블록이 놓인 자리
 * pt_block 을 1 로 잡는다.
 * 값 0x01 의 정의는 z/VM 소관이라 이 트리에서 확인 못 함. 다만 이름이
 * 같은 디렉터리의 FBA 디시플린(dasd_fba.c)이 다루는 장치 종류와 맞물린다는 점,
 * 그리고 DIAG 는 그 위에 얹혀 **실제 장치 종류와 무관하게** 블록 인터페이스를
 * 제공한다는 점이 중요하다. */
#define DEV_CLASS_FBA	0x01
/* [한국어] vdev_class 값 중 다른 하나 — ECKD 형식의 가상 장치.
 * 읽는 자는 마찬가지로 dasd_diag_check_device 이며, 이때 pt_block 을 2 로 잡는다.
 * ECKD 볼륨은 트랙과 레코드로 이루어져 있어 레이블이 놓이는 블록 번호가 FBA 와
 * 다르기 때문이다. **여기가 DIAG 의 핵심** 이다 — 밑바탕이 트랙 구조의 ECKD
 * 볼륨이어도, z/VM 이 그것을 균일한 블록 배열로 감싸 주므로 드라이버는 블록
 * 번호만 다루면 된다. 값 0x04 의 정의는 이 트리에서 확인 못 함.
 * 0x01 과 0x04 사이의 0x02·0x03 이 무엇인지도 확인 못 함이며, 드라이버는 이 둘이
 * 아닌 값을 만나면 '이 장치 종류는 DIAG 모드에서 지원하지 않는다' 며 물러선다. */
#define DEV_CLASS_ECKD	0x04

/* [한국어] 완료 외부 인터럽트의 **부코드 상위 바이트** 값 중 하나 — 31비트 형식.
 * 읽는 자는 dasd_ext_handler 하나뿐이다. 외부 인터럽트 처리기는 인자를 32비트
 * 짜리와 64비트짜리 두 개로 받는데, 어느 쪽에 우리 인터럽트 인자가 실려 왔는지를
 * 이 부코드로 가린다. 이 값이면 32비트 인자 쪽을 읽는다.
 * 비교식이 부코드를 8비트 오른쪽으로 민 값과 견주는 것에 주의하라 — 즉 부코드
 * 16비트 중 상위 8비트가 '형식', 하위 8비트가 '결과 상태' 로 쓰인다.
 * 값 0x03 의 하이퍼바이저 쪽 정의는 이 트리에서 확인 못 함. */
#define DASD_DIAG_CODE_31BIT		0x03
/* [한국어] 같은 자리의 다른 값 — 64비트 형식.
 * 이 값이면 dasd_ext_handler 가 64비트 인자 쪽에서 struct dasd_ccw_req 주소를
 * 꺼낸다. 요즘 커널은 64비트 z/Architecture 로만 빌드되고, 드라이버가 요청을
 * 낼 때 struct dasd_diag_rw_io 의 flaga 에 64비트 형식 비트를 못 박으므로
 * (아래 DASD_DIAG_FLAGA_DEFAULT 참고), 실제로 오는 것은 대개 이쪽이다.
 * 그럼에도 31비트 갈래를 남겨 둔 것은 옛 형식과의 호환 때문으로 보이며,
 * 그 판단의 근거가 될 z/VM 문서는 이 트리에서 확인 못 함.
 * 둘 중 어느 것도 아닌 부코드는 우리 것이 아니므로 처리기가 그냥 돌아간다. */
#define DASD_DIAG_CODE_64BIT		0x07

/* [한국어] struct dasd_diag_rw_io 의 flags 필드에 세우는 비트 — **비동기 요청**.
 * 설정자는 dasd_start_diag 하나뿐이다. 이 비트를 세우면 DIAG 호출이 I/O 를
 * 시작만 하고 곧바로 돌아오며(조건 코드 8), 완료는 나중에 외부 인터럽트로 온다.
 * 반대로 dasd_diag_check_device 의 인식 경로는 flags 를 0 으로 두어 **동기** 로
 * 부른다 — 그 자리에는 아직 등록된 인터럽트 처리기도, 완료를 받아 줄 요청 큐도
 * 없기 때문이다.
 * 값 0x02 가 하이퍼바이저 쪽에서 정확히 어느 비트인지는 이 트리에서 확인 못 함. */
#define DASD_DIAG_RWFLAG_ASYNC		0x02
/* [한국어] 같은 flags 필드의 다른 비트 — 캐시 우회.
 * [상류 코드 관찰] **이 심볼은 정의만 있고 쓰이는 곳이 하나도 없다.**
 * drivers 전체를 grep 해도 이 헤더의 이 줄 말고는 나오지 않는다.
 * 값 0x01 이 하이퍼바이저의 어떤 캐시 동작을 끄는지도 이 트리에서 확인 못 함.
 * 원본(1f0e418bb6)에서 확인했으며 코드는 고치지 않았다.
 * ECKD 디시플린이 명령마다 캐시 동작을 골라 넣는 것과 견주면, DIAG 판은 캐시
 * 제어를 하이퍼바이저에 통째로 맡기고 손대지 않는 셈이다. */
#define DASD_DIAG_RWFLAG_NOCACHE	0x01

/* [한국어] struct dasd_diag_init_io 와 struct dasd_diag_rw_io 가 공유하는 flaga 필드의 비트 —
 * **64비트 형식으로 다루라**.
 * 설정자: mdsk_init_io, dasd_start_diag, dasd_diag_check_device 세 곳이 모두
 * 아래 DASD_DIAG_FLAGA_DEFAULT 를 통해 이 비트를 세운다. 예외는 mdsk_term_io 로,
 * 파라미터 블록을 0 으로 지우고 장치 번호만 채운 뒤 그대로 부른다.
 * 읽는 자: 하이퍼바이저. 이 비트가 서면 블록 번호 필드를 64비트로 읽는다는 뜻으로
 * 보이며(구조체의 blocknum_t 가 u64 인 것과 앞뒤가 맞는다), 정확한 정의는
 * 이 트리에서 확인 못 함.
 * 값이 0x80 — 바이트의 최상위 비트 — 인 점에 주의하라. s390 은 빅엔디언이므로
 * 이 바이트에서 가장 먼저 오는 비트다. */
#define DASD_DIAG_FLAGA_FORMAT_64BIT	0x80

/* [한국어] 장치 특성 12바이트 — 진단 호출로 읽어 오는 가상 장치 정보
 * 
 * **이 구조체만은 드라이버가 채워서 보내고 하이퍼바이저가 채워서 돌려준다.**
 * dasd_diag_check_device 가 앞 두 필드(장치 번호와 이 구조체의 길이)만 채운 뒤
 * 장치 특성 읽기 진단 호출에 넘기면, 나머지 여덟 바이트가 채워져 돌아온다.
 * 
 * 주의할 점이 하나 있다. 그 호출에 넘길 때 **struct diag210 포인터로 형변환** 한다.
 * struct diag210 은 arch/s390 소관인데 이 트리에는 그 계층이 없으므로, 두 구조체의
 * 배치가 실제로 같은지는 이 트리에서 확인 못 함. 다만 이 형변환이 성립하려면
 * 두 배치가 바이트 단위로 같아야 한다는 것만은 분명하다.
 * 
 * 크기 셈은 2+2+1x8 = 12 바이트다. aligned(4) 를 건 것은 이 구조체가 진단 호출의
 * 인자로 나가기 때문이며, packed 는 컴파일러가 필드 사이에 빈틈을 넣지 못하게 한다.
 * 
 * 읽는 자는 두 곳이다. dasd_diag_check_device 가 vdev_class 로 레이블 블록 위치를
 * 정하고, 같은 함수가 디버그 로그에 네 값을 찍는다. 그리고 dasd_diag_fill_info 가
 * 이 구조체를 통째로 사용자 공간의 장치 정보 버퍼에 복사한다. */
struct dasd_diag_characteristics {
	/* [한국어] 가상 장치 번호(z/VM 게스트가 보는 장치 주소).
	 * 설정자: dasd_diag_check_device 가 채널 장치의 식별자에서 얻은 devno 를 넣는다.
	 * 그 devno 는 struct dasd_diag_private 의 dev_id 에 장치 인식 때 한 번 담아 둔 것이다.
	 * 읽는 자: 하이퍼바이저가 '어느 장치의 특성을 물어보는지' 를 이 값으로 안다.
	 * 돌아온 뒤에는 드라이버 자신도 디버그 로그에 이 값을 찍는다.
	 * 값 범위: 16비트 장치 번호. 리눅스가 쓰는 버스 ID 문자열의 마지막 네 자리에 해당한다.
	 * 동기화: 장치마다 하나뿐인 사적 구조체 안의 버퍼이며, 장치 인식 중에만 쓰이므로
	 * 경쟁이 없다. */
	u16 dev_nr;
	/* [한국어] 이 구조체의 길이(바이트). 하이퍼바이저에게 '여기까지만 채우라' 고 알린다.
	 * 설정자: dasd_diag_check_device 가 sizeof 로 계산해 넣는다 — 즉 12 다.
	 * 읽는 자: 하이퍼바이저. 버퍼가 넘치지 않게 하는 안전장치로 보인다.
	 * 값 범위: 12 하나뿐이다. 드라이버가 다른 값을 넣는 자리가 없다.
	 * 동기화: 위와 같다. */
	u16 rdc_len;
	/* [한국어] **가상** 장치의 종류. 이 구조체에서 드라이버가 실제로 판단에 쓰는 유일한 필드다.
	 * 설정자: 하이퍼바이저.
	 * 읽는 자: dasd_diag_check_device 의 분기문. 위의 DEV_CLASS_FBA 면 pt_block 을 1,
	 * DEV_CLASS_ECKD 면 2 로 잡고, 그 밖의 값이면 지원하지 않는 장치라며 물러선다.
	 * 값 범위: 0x01 또는 0x04 만 처리한다. 나머지 값의 뜻은 이 트리에서 확인 못 함.
	 * 동기화: 위와 같다. */
	u8 vdev_class;
	/* [한국어] 가상 장치의 세부 형식(모델).
	 * 설정자: 하이퍼바이저.
	 * 읽는 자: dasd_diag_check_device 가 디버그 로그 한 줄에 찍는 것이 전부다.
	 * 판단에는 쓰지 않는다.
	 * 값 범위: z/VM 이 정하는 값이라 이 트리에서 확인 못 함.
	 * 동기화: 위와 같다. */
	u8 vdev_type;
	/* [한국어] 가상 장치의 상태.
	 * 설정자: 하이퍼바이저.
	 * 읽는 자: [상류 코드 관찰] **이 디렉터리 어디에서도 읽지 않는다.**
	 * grep 으로 확인했으며, dasd_diag_fill_info 가 구조체를 통째로 복사할 때
	 * 사용자 공간으로 나가는 것이 유일한 쓰임이다.
	 * 원본(1f0e418bb6)에서 확인했으며 코드는 고치지 않았다.
	 * 값 범위: 이 트리에서 확인 못 함.
	 * 동기화: 위와 같다. */
	u8 vdev_status;
	/* [한국어] 가상 장치의 플래그.
	 * 설정자: 하이퍼바이저.
	 * 읽는 자: 위 vdev_status 와 같다 — 드라이버는 판단에 쓰지 않고, 사용자 공간으로
	 * 통째로 넘길 때만 실려 나간다. 읽기 전용 미니디스크 여부 같은 정보가 여기 있을
	 * 법하지만, 드라이버는 그것을 이 필드가 아니라 환경 초기화의 반환값 4 로 알아낸다.
	 * 값 범위: 이 트리에서 확인 못 함.
	 * 동기화: 위와 같다. */
	u8 vdev_flags;
	/* [한국어] **실제** 장치의 종류. 가상 장치를 떠받치는 진짜 디스크 쪽 정보다.
	 * 설정자: 하이퍼바이저.
	 * 읽는 자: 드라이버는 판단에 쓰지 않는다.
	 * 값 범위: 이 트리에서 확인 못 함.
	 * 동기화: 위와 같다.
	 * 가상과 실제를 나눠 알려 주는 것 자체가 DIAG 경로의 성격을 드러낸다 — 게스트가
	 * 보는 장치는 z/VM 이 만들어 낸 것이고, 그 밑에 다른 모양의 진짜 장치가 있다. */
	u8 rdev_class;
	/* [한국어] 실제 장치의 형식(모델 번호의 앞부분).
	 * 설정자: 하이퍼바이저.
	 * 읽는 자: dasd_diag_check_device 의 디버그 로그. '가상 XXXX 가 실제 XXXX/XX 위에
	 * 있다' 는 한 줄을 남겨, 관리자가 미니디스크의 밑바탕을 알 수 있게 한다.
	 * 값 범위: 이 트리에서 확인 못 함.
	 * 동기화: 위와 같다. */
	u8 rdev_type;
	/* [한국어] 실제 장치의 모델.
	 * 설정자: 하이퍼바이저.
	 * 읽는 자: 위 rdev_type 과 같은 디버그 로그 한 줄에서 슬래시 뒤의 두 자리로 찍힌다.
	 * 값 범위: 이 트리에서 확인 못 함.
	 * 동기화: 위와 같다. */
	u8 rdev_model;
	/* [한국어] 실제 장치가 지원하는 기능 비트.
	 * 설정자: 하이퍼바이저.
	 * 읽는 자: [상류 코드 관찰] **이 디렉터리 어디에서도 읽지 않는다.**
	 * grep 으로 확인했으며, 사용자 공간으로 통째로 복사될 때만 실려 나간다.
	 * 원본(1f0e418bb6)에서 확인했으며 코드는 고치지 않았다.
	 * 값 범위: 이 트리에서 확인 못 함.
	 * 동기화: 위와 같다.
	 * 이 필드가 구조체의 마지막이므로, 여기까지가 12바이트다. */
	u8 rdev_features;
/* [한국어] packed 와 aligned(4) 를 함께 건다.
 * packed 는 컴파일러가 필드 사이나 끝에 채움 바이트를 넣지 못하게 해 배치를
 * 못 박고, aligned(4) 는 구조체 전체를 4바이트 경계에 놓는다.
 * 정렬을 요구하는 까닭은 이 버퍼의 주소가 진단 호출의 인자로 하이퍼바이저에게
 * 그대로 건네지기 때문이며, 그 정렬 요구가 실제로 몇 바이트인지는 z/VM 소관이라
 * 이 트리에서 확인 못 함. 여기서는 4를 요구한다는 사실만 확인된다. */
} __attribute__ ((packed, aligned(4)));

/* [한국어] [상류 코드 관찰] 위 flaga 비트의 별칭 — 드라이버가 실제로 쓰는 기본값.
 * 정의가 **구조체 하나를 사이에 두고** 원래 비트 정의에서 떨어져 있다.
 * 같은 묶음의 다른 상수들은 위쪽에 모여 있는데 이것만 여기 놓였다.
 * 원본(1f0e418bb6)에서 확인했으며 코드는 고치지 않았다.
 * 값 자체는 64비트 형식 비트 하나뿐이다. 이름이 '기본값' 인 것은, 앞으로
 * flaga 에 다른 비트가 생기더라도 드라이버가 늘 세워야 하는 조합을 한 이름으로
 * 묶어 두려는 뜻으로 읽힌다. 실제로 mdsk_init_io, dasd_start_diag,
 * dasd_diag_check_device 세 곳이 모두 개별 비트가 아니라 이 이름을 쓴다. */
#define DASD_DIAG_FLAGA_DEFAULT		DASD_DIAG_FLAGA_FORMAT_64BIT

/* [한국어] 블록 번호의 자료형 — 부호 없는 64비트.
 * 왜 typedef 를 두었는가: DIAG 인터페이스에는 31비트 형식과 64비트 형식이 있는데,
 * 이 드라이버는 64비트 형식만 쓰기로 정했다(위 flaga 기본값 참고). 그 결정을
 * u64 라고 여기저기 적는 대신 이름 하나로 묶어, 블록 번호를 다루는 자리마다
 * 같은 폭이 쓰이도록 못 박은 것이다.
 * 쓰이는 자리: 아래 세 구조체의 블록 번호 필드 넷과, dasd_diag.c 의 사적 구조체가
 * 들고 있는 레이블 블록 위치, 그리고 mdsk_init_io 의 인자·출력 변수다. */
typedef u64 blocknum_t;
/* [한국어] 같은 것의 부호 있는 판 — s64.
 * [상류 코드 관찰] **쓰이는 자리가 단 하나뿐이다** — 아래 struct dasd_diag_init_io 의
 * start_block 필드이며, 그 필드조차 드라이버가 채우지도 읽지도 않는다
 * (drivers 전체 grep 으로 확인했다).
 * 원본(1f0e418bb6)에서 확인했으며 코드는 고치지 않았다.
 * 블록 번호에 음수가 필요한 까닭 — 이를테면 오프셋을 거꾸로 세는 규약 — 은
 * z/VM 인터페이스 소관이라 이 트리에서 확인 못 함. */
typedef s64 sblocknum_t;

/* [한국어] 블록 서술자 24바이트 — DIAG 판의 '채널 프로그램' 한 칸
 * 
 * **ECKD·FBA 디시플린의 struct ccw1 에 해당하는 자리** 다. 다만 CCW 가 채널에게
 * 내리는 '명령' 이라면, 이쪽은 하이퍼바이저에게 건네는 '이 블록을 여기로' 라는
 * 서술에 가깝다. 명령 사슬 연결 비트도, 데이터 길이도, 표시자 비트도 없다.
 * 길이가 없는 까닭은 블록 크기가 세션 단위로 이미 정해져 있기 때문이다 —
 * 환경을 열 때 건넨 block_size 가 모든 서술자에 그대로 적용된다.
 * 
 * 이 서술자를 몇 개 이어 붙인 배열이 struct dasd_diag_rw_io 의 bio_list 가 가리키는
 * 대상이고, 개수는 같은 구조체의 block_count 로 알린다. 배열은 dasd_diag.c 의
 * struct dasd_diag_req 안에 가변 길이 멤버로 들어가며, 그 전체가 struct dasd_ccw_req
 * 의 data 영역에 놓인다.
 * 
 * 채우는 자리는 둘이다. dasd_diag_build_cp 가 블록 계층 요청을 훑으며 블록마다
 * 한 칸씩 채우고, dasd_diag_check_device 가 인식 때 한 칸짜리 배열을 따로 만든다.
 * 
 * 크기 셈은 1+1+2+4+8+8 = 24 바이트다. */
struct dasd_diag_bio {
	/* [한국어] 이 블록을 읽을지 쓸지. 위 MDSK_READ_REQ / MDSK_WRITE_REQ 중 하나.
	 * 설정자: dasd_diag_build_cp 가 요청 방향에 따라 고른 값을 배열 전체에 같은 값으로
	 * 넣는다. 인식 경로에서는 dasd_diag_check_device 가 읽기 값을 넣는다.
	 * 읽는 자: 하이퍼바이저.
	 * 값 범위: 0x01(쓰기) 또는 0x02(읽기). 배열 안의 서술자들이 서로 다른 방향을
	 * 가질 수 있는지는 이 트리에서 확인 못 함 — 드라이버는 한 요청 안에서 방향을
	 * 섞지 않는다. 블록 계층이 요청 하나에 읽기와 쓰기를 섞지 않기 때문이다.
	 * 동기화: 이 서술자 배열은 요청 하나에 딸린 사적 버퍼라 공유되지 않는다. */
	u8 type;
	/* [한국어] 이 블록의 처리 결과. 하이퍼바이저가 채워 돌려주는 자리다.
	 * 설정자: 하이퍼바이저.
	 * 읽는 자: [상류 코드 관찰] **드라이버는 이 필드를 읽지 않는다.**
	 * drivers 전체를 grep 해도 이 필드를 읽는 코드가 없다. 오류는 오직 DIAG 호출의
	 * 반환값과 완료 인터럽트의 부코드 하위 바이트로만 판단하며, 그래서 '여러 블록 중
	 * 몇 번째가 실패했는지' 는 이 드라이버가 알 수 없다.
	 * 원본(1f0e418bb6)에서 확인했으며 코드는 고치지 않았다.
	 * 값 범위: 이 트리에서 확인 못 함.
	 * 동기화: 위와 같다. */
	u8 status;
	/* [한국어] 예약 2바이트. type 과 status 뒤에 놓여 다음 필드를 4바이트 경계로 밀어 준다.
	 * 설정자: 직접 채우는 코드는 없다. 서술자 전체를 0 으로 지우는 과정에서 0 이 된다 —
	 * dasd_diag_build_cp 는 칸마다 지우고, dasd_diag_check_device 는 할당 때와 반복
	 * 때 두 번 지운다.
	 * 읽는 자: 하이퍼바이저가 읽는지는 이 트리에서 확인 못 함. 이름은 예약이다.
	 * 값 범위: 0.
	 * 동기화: 위와 같다. */
	u8 spare1[2];
	/* [한국어] ALET(Access List Entry Token) — 버퍼 주소가 어느 주소 공간에 속하는지 알리는 표.
	 * 설정자: [상류 코드 관찰] **이름을 짚어 채우는 코드가 하나도 없다.**
	 * 언제나 서술자를 0 으로 지우는 과정에서 0 이 된다. 즉 이 드라이버는 늘
	 * '기본 주소 공간' 을 뜻하는 0 만 쓴다.
	 * 원본(1f0e418bb6)에서 확인했으며 코드는 고치지 않았다.
	 * 읽는 자: 하이퍼바이저. 아래 buffer 주소를 어느 공간에서 풀어야 하는지 이 값으로
	 * 정하는 것으로 보이며, 정확한 규약은 이 트리에서 확인 못 함.
	 * 값 범위: 0.
	 * 동기화: 위와 같다. */
	u32 alet;
	/* [한국어] 이 서술자가 다룰 블록 번호.
	 * 설정자: dasd_diag_build_cp 가 블록 계층 요청의 시작 위치를 블록 단위로 바꾼 뒤
	 * 칸마다 1 씩 올려 가며 넣는다. **넣는 값이 계산한 번호에 1 을 더한 것** 이라는
	 * 점이 중요하다 — 즉 하이퍼바이저의 블록 번호는 1 부터 시작한다. 인식 경로에서도
	 * 레이블 블록 위치에 1 을 더해 넣는다.
	 * 읽는 자: 하이퍼바이저.
	 * 값 범위: 64비트. 1 부터 환경 초기화가 돌려준 end_block 까지.
	 * 동기화: 위와 같다. */
	blocknum_t block_number;
	/* [한국어] 이 블록을 담을(또는 담은) 메모리 버퍼의 주소.
	 * 설정자: dasd_diag_build_cp 가 블록 계층 요청의 세그먼트를 훑으며 얻은 커널 가상
	 * 주소를, 블록 크기만큼씩 밀어 가며 넣는다. 인식 경로에서는 미리 잡아 둔 한 페이지의
	 * 주소가 들어간다.
	 * 읽는 자: 하이퍼바이저가 이 주소로 직접 데이터를 옮긴다.
	 * **여기가 CCW 경로와 크게 다른 지점** 이다. 채널 경로에서는 4KB 경계를 넘는 전송을
	 * 위해 간접 주소 목록을 따로 만들어야 하지만, DIAG 판에는 그런 장치가 없다.
	 * 대신 서술자 하나가 블록 하나만 다루므로 경계 문제가 애초에 생기지 않는다.
	 * 이 주소가 물리 주소가 아니라 커널 가상 주소인 채로 건네지는 점도 눈에 띄는데,
	 * z/VM 이 게스트의 주소 변환을 알고 있기에 가능한 일로 보이며 그 근거는
	 * 이 트리에서 확인 못 함.
	 * 값 범위: 유효한 커널 주소. NULL 이 되는 경우는 없다.
	 * 동기화: 위와 같다. */
	void *buffer;
/* [한국어] packed 와 aligned(8) 를 함께 건다.
 * packed 로 배치를 못 박고, aligned(8) 로 이 서술자 배열의 각 칸이 8바이트 경계에
 * 놓이게 한다. 8을 요구하는 것은 안에 64비트 필드(블록 번호와 포인터)가 있어서이며,
 * 하이퍼바이저가 그 경계를 요구하는지 여부는 이 트리에서 확인 못 함.
 * 크기가 정확히 24바이트라 8의 배수이므로, 배열로 늘어놓아도 정렬이 유지된다. */
} __attribute__ ((packed, aligned(8)));

/* [한국어] 환경 초기화·해제 파라미터 64바이트 — DIAG 세션을 열고 닫는 인자
 * 
 * 기능 코드 INIT_BIO 와 TERM_BIO 가 함께 쓰는 하나의 구조체다. 여는 쪽에서는
 * '이 장치를 이 블록 크기로, 이 오프셋부터 다루겠다' 고 알리고, 닫는 쪽에서는
 * 장치 번호만 채운 채 그대로 넘긴다.
 * 
 * **CCW 경로에 이에 해당하는 것이 없다.** ECKD 는 명령 사슬마다 익스텐트 정의를
 * 새로 붙이지만, DIAG 는 세션을 한 번 열어 두면 그 뒤의 모든 읽기·쓰기가 그
 * 설정을 물려받는다. 그래서 블록 크기가 struct dasd_diag_bio 에 없고 여기에 있다.
 * 
 * 세션이 하나뿐이라는 사실은 오류 복구의 모양까지 정한다. 요청 하나만 취소할
 * 길이 없으므로, dasd_diag_term_IO 도 dasd_diag_erp 도 '닫고 다시 여는' 똑같은
 * 두 걸음을 밟는다.
 * 
 * 이 구조체의 실체는 장치마다 하나씩, dasd_diag.c 의 struct dasd_diag_private 안에
 * 박혀 있다. 요청마다 새로 잡지 않는 까닭은 장치당 DIAG 호출이 한 번에 하나이기
 * 때문이며, 부르는 쪽이 매번 통째로 0 으로 지우고 다시 채운다.
 * 
 * 크기 셈은 2+1+21 = 24(공통 머리), +4+4 = 32, +8+8+8 = 56, +8 = 64 바이트다.
 * 앞 24바이트가 아래 struct dasd_diag_rw_io 와 **같은 배치** 라는 점에 주목하라 —
 * 장치 번호, flaga, 21바이트 예약이 두 구조체에 똑같이 놓여 있다. */
struct dasd_diag_init_io {
	/* [한국어] 가상 장치 번호. 어느 장치의 세션을 열고 닫는지 알린다.
	 * 설정자: mdsk_init_io 와 mdsk_term_io 가 사적 구조체에 담아 둔 devno 를 넣는다.
	 * 읽는 자: 하이퍼바이저.
	 * 값 범위: 16비트 장치 번호.
	 * 동기화: 장치마다 하나뿐인 버퍼이며, 장치 단위로 직렬화된 경로에서만 쓰인다. */
	u16 dev_nr;
	/* [한국어] 형식 플래그. 위 DASD_DIAG_FLAGA_DEFAULT 를 넣어 64비트 형식을 못 박는다.
	 * 설정자: mdsk_init_io 하나뿐이다.
	 * [상류 코드 관찰] **mdsk_term_io 는 이 필드를 세우지 않는다** — 구조체를 0 으로
	 * 지우고 장치 번호만 채운 뒤 부르므로 flaga 가 0 으로 나간다. 여는 쪽과 닫는 쪽의
	 * 형식 표시가 서로 다른 셈인데, 닫기에는 블록 번호가 오가지 않으니 형식이 무의미
	 * 하기 때문으로 읽힌다. 원본(1f0e418bb6)에서 확인했으며 코드는 고치지 않았다.
	 * 읽는 자: 하이퍼바이저.
	 * 값 범위: 0 또는 0x80.
	 * 동기화: 위와 같다. */
	u8 flaga;
	/* [한국어] 예약 21바이트. 장치 번호와 flaga 뒤를 메워 다음 필드를 오프셋 24 로 민다.
	 * 설정자: 직접 채우는 코드는 없다. mdsk_init_io 와 mdsk_term_io 가 구조체를 통째로
	 * 0 으로 지우는 과정에서 0 이 된다.
	 * 읽는 자: 하이퍼바이저가 이 안을 읽는지는 이 트리에서 확인 못 함.
	 * 값 범위: 전부 0.
	 * 동기화: 위와 같다.
	 * 길이가 하필 21 인 까닭 — 즉 왜 다음 필드가 오프셋 24 여야 하는지 — 은 z/VM 이
	 * 정한 배치 소관이라 이 트리에서 확인 못 함. 확인되는 것은 아래 struct
	 * dasd_diag_rw_io 도 똑같이 오프셋 24 에서 본론이 시작한다는 사실뿐이다. */
	u8 spare1[21];
	/* [한국어] 이 세션에서 쓸 블록 크기(바이트).
	 * 설정자: mdsk_init_io 가 인자로 받은 값을 넣는다. mdsk_init_io 를 부르는 자리는
	 * 넷이다 — 장치 인식 때 512 부터 한 페이지까지 배로 늘려 가며 시험하는 곳,
	 * 그 시험이 끝난 뒤 확정된 크기로 다시 여는 곳, 오류 복구(dasd_diag_erp)에서
	 * 다시 여는 곳, 요청 중단(dasd_diag_term_IO)에서 다시 여는 곳이다.
	 * [상류 코드 관찰] mdsk_term_io 는 이 필드를 0 으로 둔 채 닫기를 부른다.
	 * 원본(1f0e418bb6)에서 확인했으며 코드는 고치지 않았다.
	 * 읽는 자: 하이퍼바이저. 이 값이 struct dasd_diag_bio 에 길이 필드가 없어도 되는
	 * 이유다.
	 * 값 범위: 512, 1024, 2048, 4096. 시험 반복문이 그 범위를 강제하고, 상위 계층의
	 * 블록 크기 검사도 같은 넷만 허용한다.
	 * 동기화: 위와 같다. */
	u32 block_size;
	/* [한국어] 예약 4바이트. block_size 뒤를 메워 다음 필드를 8바이트 경계(오프셋 32)로 민다.
	 * 설정자: 없다. 구조체를 0 으로 지울 때 0 이 된다.
	 * 읽는 자: 이 트리에서 확인 못 함.
	 * 값 범위: 전부 0.
	 * 동기화: 위와 같다.
	 * 이 채움이 없으면 뒤따르는 64비트 필드 셋이 4바이트 경계에 놓이게 된다 —
	 * packed 가 걸려 있어 컴파일러가 알아서 밀어 주지 않으므로 손으로 넣은 것이다. */
	u8 spare2[4];
	/* [한국어] 세션이 볼 첫 블록의 오프셋. 볼륨 앞부분을 건너뛰고 싶을 때 쓴다.
	 * 설정자: mdsk_init_io 가 인자로 받은 값을 넣는데, **부르는 네 자리가 모두 0 을
	 * 넘긴다.** 즉 이 드라이버는 볼륨 전체를 있는 그대로 쓰며 오프셋 기능을 쓰지 않는다.
	 * 읽는 자: 하이퍼바이저.
	 * 값 범위: 언제나 0. 다른 값을 넣는 코드가 이 트리에 없다.
	 * 동기화: 위와 같다.
	 * 이 필드가 있다는 것은 z/VM 쪽에 '미니디스크 안의 일부만 잘라 보여 주는' 기능이
	 * 있다는 뜻으로 읽히지만, 그 규약은 이 트리에서 확인 못 함. */
	blocknum_t offset;
	/* [한국어] 세션의 시작 블록 번호(부호 있는 64비트).
	 * 설정자: [상류 코드 관찰] **없다.** 이 드라이버는 이 필드를 채우지 않으며,
	 * 구조체를 0 으로 지우는 과정에서 0 으로만 나간다. 바로 위 offset 과 뜻이 어떻게
	 * 갈리는지도 이 트리에서 확인 못 함.
	 * 게다가 이 필드는 이 헤더에서 sblocknum_t(부호 있는 자료형)를 쓰는 **유일한
	 * 자리** 다. 그 자료형이 이 한 필드를 위해 존재하는 셈이다.
	 * 원본(1f0e418bb6)에서 확인했으며 코드는 고치지 않았다.
	 * 읽는 자: 하이퍼바이저.
	 * 값 범위: 언제나 0.
	 * 동기화: 위와 같다. */
	sblocknum_t start_block;
	/* [한국어] **하이퍼바이저가 채워 돌려주는 유일한 필드** — 세션이 볼 마지막 블록 번호.
	 * 설정자: 하이퍼바이저.
	 * 읽는 자: mdsk_init_io 가 호출자에게 넘겨받은 포인터에 이 값을 복사해 준다.
	 * 다만 반환값의 하위 두 비트가 0 일 때, 그리고 호출자가 포인터를 준 때만 그렇게 한다.
	 * 실제로 값을 받아 쓰는 곳은 dasd_diag_check_device 한 곳뿐으로, CMS 레이블이
	 * 없는 볼륨의 전체 블록 수로 이 값을 그대로 쓴다. 레이블이 있으면 레이블에 적힌
	 * 블록 수가 이 값을 이긴다.
	 * 값 범위: 64비트 블록 번호. 오프셋을 뺀 뒤의 개수라고 상류 주석이 밝힌다.
	 * 동기화: 위와 같다. */
	blocknum_t end_block;
	/* [한국어] 예약 8바이트. 구조체를 64바이트로 채워 맞춘다.
	 * 설정자: 없다. 0 으로 지울 때 0 이 된다.
	 * 읽는 자: 이 트리에서 확인 못 함.
	 * 값 범위: 전부 0.
	 * 동기화: 위와 같다.
	 * 이 8바이트가 없으면 구조체는 56바이트가 된다. 굳이 64로 맞춘 것은 z/VM 이
	 * 그 크기의 파라미터 블록을 기대하기 때문으로 보이며, 그 근거는 이 트리에서
	 * 확인 못 함. */
	u8  spare3[8];
/* [한국어] packed 와 aligned(8) 를 함께 건다.
 * packed 는 21바이트·4바이트 예약 배열 뒤에 컴파일러가 정렬용 빈틈을 끼워 넣지
 * 못하게 막는다. 그것이 없으면 오프셋 24 의 block_size 부터 배치가 어긋나
 * 하이퍼바이저가 엉뚱한 자리를 읽는다.
 * aligned(8) 은 이 파라미터 블록 전체가 8바이트 경계에 놓이게 한다. 이 구조체는
 * 장치의 사적 구조체 안에 박혀 있으므로, 그 안에서의 위치까지 이 속성이 정한다. */
} __attribute__ ((packed, aligned(8)));

/* [한국어] 읽기·쓰기 파라미터 64바이트 — DIAG 판의 '채널 프로그램 시작 인자'
 * 
 * 기능 코드 RW_BIO 로 부를 때 넘기는 구조체다. 앞의 struct dasd_diag_init_io 와
 * **앞 24바이트가 똑같고**(장치 번호, flaga, 21바이트 예약), 그 뒤가 다르다.
 * 
 * CCW 경로와 견주면 이 구조체는 '채널 프로그램의 주소와 인터럽트 인자를 담아
 * 채널을 시작하는 명령' 의 자리에 있다. 다만 담기는 것이 CCW 사슬의 머리가 아니라
 * struct dasd_diag_bio 배열의 머리이고, 개수를 따로 알려야 한다는 점이 다르다.
 * CCW 사슬은 각 CCW 의 연결 비트로 끝을 알리지만, 이 배열에는 그런 표시가 없다.
 * 
 * 부르는 자리는 둘이고 성격이 다르다.
 *  - dasd_start_diag: 정상 I/O. flags 에 비동기 비트를 세우고, interrupt_params 에
 *    요청의 주소를 실어 보낸다. 완료는 외부 인터럽트로 온다.
 *  - dasd_diag_check_device: 장치 인식. flags 를 0 으로 두어 동기로 부르고,
 *    interrupt_params 도 0 으로 둔다. 아직 인터럽트를 받을 준비가 없기 때문이다.
 * 
 * 이 구조체의 실체도 장치마다 하나씩, dasd_diag.c 의 struct dasd_diag_private 안에
 * 박혀 있다. **장치당 진행 중인 DIAG 호출이 하나뿐** 이라는 전제 위에 선 설계다.
 * 
 * 크기 셈은 2+1+21 = 24(공통 머리), +1+1+2 = 28, +4+4+4 = 40, +8+8 = 56, +8 = 64. */
struct dasd_diag_rw_io {
	/* [한국어] 가상 장치 번호.
	 * 설정자: dasd_start_diag 와 dasd_diag_check_device 가 각각 넣는다. 앞은 사적
	 * 구조체의 devno 를, 뒤는 장치 특성에서 읽어 온 dev_nr 을 넣는데 두 값은 같다.
	 * 읽는 자: 하이퍼바이저.
	 * 값 범위: 16비트 장치 번호.
	 * 동기화: 장치마다 하나뿐인 버퍼다. dasd_start_diag 는 장치의 채널 장치 락 아래에서
	 * 불리므로 같은 장치에 대해 두 번 겹쳐 채워지지 않는다. */
	u16 dev_nr;
	/* [한국어] 형식 플래그. 위 DASD_DIAG_FLAGA_DEFAULT 를 넣어 64비트 형식을 못 박는다.
	 * 설정자: dasd_start_diag 와 dasd_diag_check_device 둘 다 넣는다. 여기서는 앞의
	 * 초기화 구조체와 달리 **두 경로가 모두** 세운다.
	 * 읽는 자: 하이퍼바이저. 아래 bio_list 가 가리키는 서술자의 블록 번호를 64비트로
	 * 읽으라는 뜻으로 보인다.
	 * 값 범위: 0x80.
	 * 동기화: 위와 같다. */
	u8  flaga;
	/* [한국어] 예약 21바이트. 앞의 초기화 구조체와 같은 자리, 같은 길이다.
	 * 설정자: 없다. 부르는 쪽이 구조체를 0 으로 지우거나(인식 경로) 사적 구조체가
	 * 장치 할당 때 0 으로 시작한 뒤 필요한 필드만 덮어쓴다(정상 경로).
	 * [상류 코드 관찰] dasd_start_diag 는 이 구조체를 **다시 지우지 않고** 필요한
	 * 일곱 필드만 덮어쓴다. 요청마다 값이 바뀌는 필드가 그 일곱뿐이고 나머지는
	 * 한 번 0 이면 계속 0 이기 때문이다.
	 * 원본(1f0e418bb6)에서 확인했으며 코드는 고치지 않았다.
	 * 읽는 자: 이 트리에서 확인 못 함.
	 * 값 범위: 전부 0.
	 * 동기화: 위와 같다. */
	u8  spare1[21];
	/* [한국어] 저장 키(storage key) — 이 전송에 쓸 접근 보호 키.
	 * 설정자: dasd_start_diag 와 dasd_diag_check_device 가 **둘 다 0 을 못 박는다.**
	 * 읽는 자: 하이퍼바이저. s390 의 저장 보호 방식에서 접근 권한을 가리는 값으로
	 * 보이며, 0 이 정확히 무엇을 뜻하는지는 arch/s390 과 z/VM 소관이라 이 트리에서
	 * 확인 못 함.
	 * 값 범위: 언제나 0.
	 * 동기화: 위와 같다. */
	u8  key;
	/* [한국어] 전송 방식 플래그. 위 DASD_DIAG_RWFLAG_ 계열 비트가 들어가는 자리다.
	 * 설정자: **이 필드가 두 경로를 가르는 스위치** 다.
	 * dasd_start_diag 는 비동기 비트를 세워 '시작만 하고 돌아오라' 고 하고,
	 * dasd_diag_check_device 는 0 을 넣어 '끝날 때까지 돌아오지 말라' 고 한다.
	 * 후자에는 상류 주석 'do synchronous io' 가 붙어 있다.
	 * 읽는 자: 하이퍼바이저.
	 * 값 범위: 0 또는 0x02. 캐시 우회 비트 0x01 은 정의만 있고 쓰이지 않는다.
	 * 동기화: 위와 같다. */
	u8  flags;
	/* [한국어] 예약 2바이트. key 와 flags 뒤를 메워 다음 필드를 4바이트 경계(오프셋 28)로 민다.
	 * 설정자: 없다.
	 * 읽는 자: 이 트리에서 확인 못 함.
	 * 값 범위: 전부 0.
	 * 동기화: 위와 같다. */
	u8  spare2[2];
	/* [한국어] 아래 bio_list 가 가리키는 서술자 배열의 **칸 수**.
	 * 설정자: dasd_start_diag 가 dasd_diag_build_cp 때 세어 둔 블록 수를 그대로 옮긴다.
	 * 인식 경로는 1 을 넣는다(레이블 블록 한 개만 읽으므로).
	 * 읽는 자: 하이퍼바이저. CCW 사슬과 달리 배열에 끝 표시가 없으므로, 이 값이
	 * 유일한 경계다. 이 값이 틀리면 하이퍼바이저가 배열 밖을 읽는다.
	 * 값 범위: 1 이상. 상한은 dasd_diag.c 의 DIAG_MAX_BLOCKS 로 정해지며, 그 값은
	 * 장치의 정적 메모리 풀 두 페이지에 요청 두 개가 들어가도록 역산한 것이다.
	 * 동기화: 위와 같다. */
	u32 block_count;
	/* [한국어] ALET — 아래 bio_list 주소가 속한 주소 공간의 표.
	 * 설정자: [상류 코드 관찰] **이름을 짚어 채우는 코드가 없다.** 언제나 0 이다.
	 * struct dasd_diag_bio 안의 같은 이름 필드와 짝을 이루는데, 그쪽도 마찬가지로
	 * 0 으로만 나간다. 원본(1f0e418bb6)에서 확인했으며 코드는 고치지 않았다.
	 * 읽는 자: 하이퍼바이저.
	 * 값 범위: 0.
	 * 동기화: 위와 같다. */
	u32 alet;
	/* [한국어] 예약 4바이트. alet 뒤를 메워 다음 필드를 8바이트 경계(오프셋 40)로 민다.
	 * 설정자: 없다.
	 * 읽는 자: 이 트리에서 확인 못 함.
	 * 값 범위: 전부 0.
	 * 동기화: 위와 같다. */
	u8  spare3[4];
	/* [한국어] **완료 인터럽트에 실려 돌아올 값** — 이 드라이버는 요청의 주소를 넣는다.
	 * 설정자: dasd_start_diag 가 struct dasd_ccw_req 포인터를 정수로 바꿔 넣는다.
	 * 인식 경로는 0 을 넣는다.
	 * 읽는 자: 하이퍼바이저가 이 값을 그대로 간직했다가, 완료 외부 인터럽트의 인자로
	 * 되돌려준다. 그것을 받는 쪽이 dasd_ext_handler 이며, 받은 값을 다시 포인터로 바꿔
	 * 어느 요청이 끝났는지 알아낸다.
	 * **CCW 경로의 intparm 과 같은 구실** 이다. 다만 채널 경로에서는 채널 서브시스템이
	 * 그 값을 장치별 인터럽트에 실어 주는 반면, 여기서는 시스템 전체에 하나뿐인 외부
	 * 인터럽트 처리기가 이 값만 보고 주인을 찾아야 한다. 그래서 처리기는 값을 믿기 전에
	 * 요청 안의 매직 값이 디시플린 이름과 맞는지 반드시 대조한다.
	 * 값 범위: 유효한 커널 포인터, 또는 인식 경로의 0. 0 으로 돌아온 인터럽트는
	 * '요청 없는 인터럽트' 로 보고 버려진다.
	 * 동기화: 위와 같다. */
	u64 interrupt_params;
	/* [한국어] 서술자 배열의 첫 칸 주소.
	 * 설정자: dasd_start_diag 가 요청의 데이터 영역 안에 만들어 둔 배열의 머리를 넣는다.
	 * 인식 경로는 따로 잡은 한 칸짜리 서술자의 주소를 넣는다.
	 * 읽는 자: 하이퍼바이저가 이 주소에서 block_count 칸만큼 읽어 내려간다.
	 * 값 범위: 유효한 커널 주소.
	 * 동기화: 위와 같다.
	 * 정상 경로에서 이 배열은 struct dasd_ccw_req 의 data 영역, 곧 장치의 정적 메모리
	 * 풀에서 떼어 온 조각 안에 있다. 그 풀을 쓰는 까닭은 인터럽트 문맥에서도 요청을
	 * 만들 수 있어야 하기 때문이며, 그 사정은 dasd_int.h 의 덩어리 할당기 주석에 있다. */
	struct dasd_diag_bio *bio_list;
	/* [한국어] 예약 8바이트. 구조체를 64바이트로 채워 맞춘다.
	 * 설정자: 없다.
	 * 읽는 자: 이 트리에서 확인 못 함.
	 * 값 범위: 전부 0.
	 * 동기화: 위와 같다.
	 * 앞의 초기화 구조체도 같은 자리에 같은 길이의 예약을 두어 64바이트로 끝난다 —
	 * 두 파라미터 블록의 크기를 맞춰 둔 것이다. */
	u8  spare4[8];
/* [한국어] packed 와 aligned(8) 를 함께 건다. 앞의 초기화 구조체와 같은 이유다.
 * packed 가 없으면 21바이트 예약 뒤부터 배치가 어긋나고, aligned(8) 이 없으면
 * 64비트 필드 둘(인터럽트 인자와 포인터)이 어긋난 경계에 놓일 수 있다.
 * 이 구조체가 이 헤더의 마지막이며, 헤더에는 중복 포함 방지 가드가 없다 —
 * 포함하는 파일이 dasd_diag.c 하나뿐이라 문제가 되지 않는다. */
} __attribute__ ((packed, aligned(8)));
